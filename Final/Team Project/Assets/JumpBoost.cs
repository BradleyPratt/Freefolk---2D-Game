using UnityEngine;
using System.Collections;


public class JumpBoost : MonoBehaviour {

/*	public float jump_strength = 5.0;
	public float jump_timer = 2.5;

	void OnControllerColliderHit(hit ControllerColliderHit) {
		if(hit. gameObject.tag == "Boost") {
			jump_strength *= 2.0;
			Invoke("ResetJump", jump_timer);
		}
	}

	void ResetJump() {
		jump_strength *= 0.5;
	}
    */
}